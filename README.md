# ofc.bio — páginas de links

- `/joice` mantém a página pública anterior.
- `/maya` usa o mesmo visual temporariamente, com nome, @ e Telegram da Maya.

Para publicar em `ofc.bio`, conecte este repositório ao projeto Vercel que administra o domínio. O link `/maya` já está publicado provisoriamente em `https://privacy-joice.vercel.app/maya`.
